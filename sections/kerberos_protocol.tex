% SEÇÃO 3: O PROTOCOLO KERBEROS
\section{O Protocolo Kerberos}

O Kerberos é um protocolo de autenticação de rede projetado para fornecer autenticação forte para aplicações cliente/servidor usando criptografia de chave secreta. A versão 5 (V5), definida na RFC 4120 \cite{rfc4120}, é o padrão atual da indústria e resolve o problema de transmitir identidades através de uma rede insegura sem expor senhas.

\subsection{Origem e Significado}
O nome do protocolo é uma referência direta à mitologia grega: Cérbero (\textit{Kerberos}), o cão de três cabeças que guardava os portões do submundo (Hades). A analogia reflete a arquitetura de confiança de três pontas do protocolo, onde as ``cabeças'' representam o Cliente, o Servidor e o KDC (Key Distribution Center), que juntos vigiam o acesso à rede.

Historicamente, o protocolo foi desenvolvido no MIT (\textit{Massachusetts Institute of Technology}) em meados da década de 1980 como parte do \textbf{Projeto Athena}. O objetivo era criar um ambiente educacional distribuído onde estudantes pudessem acessar seus arquivos de qualquer estação de trabalho sem comprometer a segurança da rede. Baseado nas propostas teóricas de Needham e Schroeder, o Kerberos foi a solução encontrada para impedir que senhas trafegassem em texto claro, prevenindo ataques de interceptação (\textit{sniffing}) \cite{neuman1994}.

\subsection{Arquitetura e Componentes}
O ecossistema Kerberos opera baseando-se em tickets e é composto pelas três entidades lógicas mencionadas na analogia mitológica:

\begin{itemize}
    \item \textbf{Cliente (Principal):} A entidade (usuário ou software) que deseja acessar um recurso. Cada cliente possui uma chave secreta $K_c$, geralmente derivada do hash de sua senha.
    \item \textbf{Servidor de Aplicação (SS - Service Server):} O recurso que o cliente deseja acessar (ex: servidor de arquivos, banco de dados). O servidor compartilha uma chave secreta $K_s$ exclusivamente com o KDC.
    \item \textbf{KDC (Key Distribution Center):} A terceira parte confiável (\textit{Trusted Third Party}). O KDC mantém um banco de dados com as chaves secretas de todos os usuários e serviços. Logicamente, ele é dividido em dois subcomponentes funcionais:
    \begin{itemize}
        \item \textit{Authentication Server (AS):} Responsável pelo login inicial. Ele verifica a senha do usuário e emite o Ticket de Concessão de Tickets (TGT).
        \item \textit{Ticket Granting Server (TGS):} Responsável por emitir tickets de serviço específicos para clientes que já possuem um TGT válido, viabilizando o recurso de \textit{Single Sign-On}.
    \end{itemize}
\end{itemize}

\subsection{Fluxo de Mensagens e Chaves de Sessão}

O funcionamento do Kerberos pode ser dividido em três fases distintas: a autenticação inicial, a obtenção de autorização para serviços e o acesso ao recurso. A Figura \ref{fig:kerberos_flow} ilustra a troca de mensagens entre as entidades.
\begin{figure}[h]
\centering
\begin{tikzpicture}[
    % Aumentamos a distância e o tamanho padrão das caixas
    node distance=6cm,
    entity/.style={rectangle, draw, rounded corners, fill=blue!10, minimum width=3cm, minimum height=4cm, font=\bfseries, align=center},
    server/.style={rectangle, draw, rounded corners, fill=green!10, minimum width=3cm, minimum height=1.5cm, font=\bfseries},
    msg/.style={-{Stealth[scale=1.0]}, thick, font=\footnotesize}
]

% --- NODES ---
% Cliente e KDC mais altos para caber as 4 setas confortavelmente
\node[entity] (client) {Cliente};
\node[entity, fill=red!10] (kdc) [right=6cm of client] {KDC\\(AS + TGS)};

% Servidor posicionado abaixo, no centro (formato triangular)
% Usamos a biblioteca calc para achar o meio: ($ (A)!0.5!(B) $) e descemos 5cm
\node[server] (server) at ($(client)!0.5!(kdc) + (0,-5)$) {Servidor (SS)};

% --- MESSAGES (CLIENTE <-> KDC) ---
% Usamos yshift para distribuir verticalmente dentro da altura do nó

% 1. AS_REQ (Topo)
\draw[msg] ($(client.east)+(0, 1.5)$) -- ($(kdc.west)+(0, 1.5)$) 
    node[midway, above] {1. AS\_REQ (ID)};

% 2. AS_REP
\draw[msg] ($(kdc.west)+(0, 0.8)$) -- ($(client.east)+(0, 0.8)$) 
    node[midway, above] {2. AS\_REP ($T_{TGS}$, $K_{c,tgs}$)};

% --- Divisor visual opcional (linha pontilhada) ---
\draw[dotted, gray] ($(client.east)+(0.5, 0)$) -- ($(kdc.west)+(-0.5, 0)$);

% 3. TGS_REQ
\draw[msg] ($(client.east)+(0, -0.8)$) -- ($(kdc.west)+(0, -0.8)$) 
    node[midway, above] {3. TGS\_REQ ($T_{TGS}$, Aut)};

% 4. TGS_REP (Fundo)
\draw[msg] ($(kdc.west)+(0, -1.5)$) -- ($(client.east)+(0, -1.5)$) 
    node[midway, above] {4. TGS\_REP ($T_{Serv}$, $K_{c,s}$)};

% --- MESSAGES (CLIENTE <-> SERVIDOR) ---

% 5. AP_REQ (Curvado levemente para a esquerda)
\draw[msg] (client.south) to[bend right=15] 
    node[midway, left, align=right, xshift=-0.2cm] {5. AP\_REQ \\ ($T_{Serv}$, Aut)} 
    (server.west);

% 6. AP_REP (Curvado levemente para a direita - vindo do servidor)
\draw[msg] (server.west) to[bend right=15] 
    node[midway, right, align=left, font=\scriptsize] {6. AP\_REP (Opcional)} 
    (client.south);

\end{tikzpicture}
\caption{Fluxo de mensagens do protocolo Kerberos com detalhamento das chaves.}
\label{fig:kerberos_flow}
\end{figure}

O detalhamento das etapas descritas na Figura \ref{fig:kerberos_flow} segue abaixo, destacando o uso das chaves criptográficas:

\begin{enumerate}
    \item \textbf{Autenticação Inicial (AS):} o cliente envia uma solicitação em texto claro contendo seu ID para o AS. O AS verifica se o usuário existe em seu banco de dados.
    
    \item \textbf{Emissão do TGT:} o AS gera uma chave de sessão temporária ($K_{c,tgs}$) para comunicação futura com o TGS. O AS responde com duas mensagens:
    \begin{itemize}
        \item Mensagem A: cifrada com a chave do usuário ($K_c$). Contém a chave de sessão $K_{c,tgs}$.
        \item Mensagem B (TGT): cifrada com a chave secreta do TGS ($K_{TGS}$). Contém o ID do cliente, validade e a chave de sessão $K_{c,tgs}$.
    \end{itemize}
    \small{\textit{Nota:} o cliente decifra a Mensagem A usando sua senha. Se a senha estiver correta, ele obtém a chave de sessão. O TGT permanece cifrado e opaco para o cliente.}

    \item \textbf{Solicitação de Serviço (TGS):} para acessar um recurso (ex: servidor de arquivos), o cliente envia ao TGS o TGT (obtido no passo anterior) e um \textit{Autenticador} (cifrado com $K_{c,tgs}$ contendo ID e Timestamp).
    
    \item \textbf{Emissão do Ticket de Serviço:} o TGS decifra o TGT com sua chave privada, recupera $K_{c,tgs}$ e usa essa chave para decifrar o Autenticador. Se validado, o TGS gera uma nova chave de sessão para o serviço específico ($K_{c,s}$) e envia um Ticket de Serviço cifrado com a chave do Servidor de Aplicação ($K_s$).

    \item \textbf{Acesso ao Recurso:} o cliente envia o Ticket de Serviço e um novo Autenticador ao Servidor de Aplicação. O servidor usa sua chave $K_s$ para abrir o ticket, obter a chave de sessão $K_{c,s}$ e validar o usuário \cite{tanenbaum2017}.
\end{enumerate}

% Subseção: Vulnerabilidades e Mitigações - SAMICO 
\input{sections/kerberos_mitigation}